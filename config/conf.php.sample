<?php

$version = '1.1';

# 1 or a pool of databases in master-slave conf to connect to
$pool = array('localhost',);

# Database name and credentials
$db = 'elearning';
$user = 'root';
$pass = 'root';

# Google service account path
$google_service_path = '';